= form_for @comune do |f|
  - if @comune.errors.any?
    #error_explanation
      %h2= "#{pluralize(@comune.errors.count, "error")} prohibited this comune from being saved:"
      %ul
        - @comune.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :region
    = f.text_field :region
  .actions
    = f.submit 'Save'
